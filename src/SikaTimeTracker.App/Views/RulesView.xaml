<UserControl
    x:Class="SikaTimeTracker.Views.RulesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <ScrollViewer>
            <StackPanel Padding="32,24,32,40" Spacing="20">
                <Grid>
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource PageTitleStyle}" Text="分类规则" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="为每个分类维护一组正则表达式，自动匹配进程名称或窗口标题" />
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal" Spacing="8">
                        <Button Click="OnNewCategoryClicked" Style="{StaticResource AccentButtonStyle}" Content="新建分类" />
                    </StackPanel>
                </Grid>

                <InfoBar
                    x:Name="PageMessage"
                    IsClosable="True"
                    IsOpen="False"
                    Severity="Informational" />

                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="每行填写一个正则，例如 unity、rider、code、visual studio。分类排序靠前时，其规则优先匹配。"
                    TextWrapping="Wrap" />

                <ListView x:Name="CategoryRuleList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,12" Style="{StaticResource CardStyle}">
                                <StackPanel Spacing="14">
                                    <Grid ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Width="14" Height="14" VerticalAlignment="Center" Fill="{Binding ColorBrush}" />
                                        <StackPanel Grid.Column="1" Spacing="2">
                                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Name}" />
                                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding Description}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6">
                                            <Button Click="OnEditCategoryRulesClicked" Content="编辑正则列表" Tag="{Binding}" Visibility="{Binding EditVisibility}" />
                                            <Button Click="OnEditCategoryClicked" Tag="{Binding}" ToolTipService.ToolTip="编辑分类" Visibility="{Binding EditVisibility}">
                                                <FontIcon Glyph="&#xE70F;" />
                                            </Button>
                                            <Button Click="OnDeleteCategoryClicked" Tag="{Binding}" ToolTipService.ToolTip="删除分类" Visibility="{Binding EditVisibility}">
                                                <FontIcon Glyph="&#xE74D;" />
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                    <StackPanel Spacing="10" Visibility="{Binding RulesVisibility}">
                                        <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
                                        <TextBlock FontWeight="SemiBold" Text="正则列表" />
                                        <ItemsControl ItemsSource="{Binding Patterns}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,0,0,6" Padding="10,7" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="5">
                                                        <TextBlock FontFamily="Cascadia Mono" Text="{Binding}" TextWrapping="Wrap" />
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding EmptyText}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
        <ProgressRing x:Name="BusyIndicator" Width="36" Height="36" IsActive="False" Visibility="Collapsed" />
    </Grid>
</UserControl>
